<h4>These are the questions [<%= @user.email %>] have contributed to solve:</h4>

<table>
  <tbody>
    <% @user.pages.each do |p| %>
      <tr class='rowlist'>
        <td><%= p.question %></td>
        <td><%= link_to 'Show', p %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<div class='footer'>
  <%= link_to 'All Questions', pages_path %>
  <% if current_user %>
    <%= link_to "Sign Out", destroy_user_session_url, method: 'delete' %>
  <% else %>
    <%= link_to "Sign In", new_user_session_url %>
  <% end %>
</div>
